@model UI.Models.DTOs.BookDTO
@{
    ViewData["Title"] = "Підтвердження замовлення";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">
                        <i class="fas fa-check-circle"></i> Підтвердження замовлення
                    </h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-success">
                        <h5><i class="fas fa-thumbs-up"></i> Замовлення успішно створено!</h5>
                        <p class="mb-0">Ваше замовлення було зареєстровано в системі.</p>
                    </div>

                    <h6>Деталі замовлення:</h6>
                    <dl class="row">
                        <dt class="col-sm-3">Книга:</dt>
                        <dd class="col-sm-9"><strong>@Model.Title</strong></dd>

                        <dt class="col-sm-3">Автор:</dt>
                        <dd class="col-sm-9">@Model.Author</dd>

                        <dt class="col-sm-3">Жанр:</dt>
                        <dd class="col-sm-9">@Model.GetGenreDisplayNameUkrainian()</dd>

                        <dt class="col-sm-3">Тип:</dt>
                        <dd class="col-sm-9">@Model.GetTypeDisplayNameUkrainian()</dd>

                        <dt class="col-sm-3">Дата замовлення:</dt>
                        <dd class="col-sm-9">@DateTime.Now.ToString("dd MMMM yyyy 'о' HH:mm")</dd>

                        <dt class="col-sm-3">Статус:</dt>
                        <dd class="col-sm-9"><span class="badge bg-success">Активне</span></dd>
                    </dl>

                    <div class="alert alert-info">
                        <h6><i class="fas fa-info-circle"></i> Що далі?</h6>
                        <ul class="mb-0">
                            <li>Ваше замовлення зарезервовано за вами</li>
                            <li>Ви можете переглянути всі свої замовлення в розділі "Мої замовлення"</li>
                            <li>При необхідності зверніться до адміністратора бібліотеки</li>
                        </ul>
                    </div>
                </div>
                <div class="card-footer">
                    <a asp-action="MyOrders" class="btn btn-primary">
                        <i class="fas fa-list"></i> Мої замовлення
                    </a>
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="fas fa-book"></i> Переглянути інші книги
                    </a>
                    <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-left"></i> Назад до книги
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>